First Import Libraries like pandas
Import CSV File
Use Head, TAIL Function to identify first and last coloums
Use info function to get more information of data
check the null values present in the data set
Check the duplicated values and remove all duplicates form the data set
then at last remove all the non-null values from the dataset
and save the data to cleanrd data file
